name: Scheduled image builds

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  deepface:
    uses: ./.github/workflows/remote-repo-build-image.yml
    secrets: inherit
    with:
      owner: serengil
      repo: deepface
